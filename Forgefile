# Uptime Kuma (status.l3s.me) — pre-built image deploy

on push("main", "master") {
  stage deploy {
    condition on_push
    deploy {
      name = "uptime-kuma"
      domain = "status.l3s.me"
      port = 3001
      compose_file = "compose.yaml"
    }
  }
}
